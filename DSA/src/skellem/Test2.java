package skellem;

import java.util.HashMap;
import java.util.HashSet;

public class Test2 {

	public static void main(String[] args) {
		String str = null;
		Object obj = null;
		System.out.println(str);
		System.out.println(obj);
		
		/*
		 * int[][] arr = { {'a','b','c','d'}, {'b','c','d','a'}, {'c','d','a','b'},
		 * {'d','a','b','c'} }; String str = "abcd"; HashSet<Character> allChars = new
		 * HashSet<>(); HashMap<Integer, HashSet<Character>> charInRow = new
		 * HashMap<>(); HashMap<Integer, HashSet<Character>> charInCol = new
		 * HashMap<>();
		 * 
		 * for(int i=0;i<4;i++) { for(int j=0;j<4;j++) { allChars.add((char) arr[i][j]);
		 * charInRow.putIfAbsent(i,); charInCol.putIfAbsent(); } }
		 * 
		 * for(int i=0;i<str.length();i++) { if(!(allChars.contains(str.charAt(i)) &&
		 * charInCol.containsKey(charInCol) )) }
		 */
		
		
	}

}
